"""Task 2 compressed-simulation reproduction utilities.

This package marker keeps the existing ``task2_code.*`` import surface stable.
It intentionally avoids importing submodules so package import has no Cirq,
NumPy, or filesystem side effects.
"""
